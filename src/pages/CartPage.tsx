import React from 'react';
import {Link} from "react-router-dom";

const CartPage = () => {
    return (
        <div>
            <Link to='/'>Home</Link>
            Cart
        </div>
    );
};

export default CartPage;
